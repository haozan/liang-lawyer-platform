require 'rails_helper'

RSpec.describe Reconciliation, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:reconciliation)
  #   expect(record).to be_persisted
  # end
end
