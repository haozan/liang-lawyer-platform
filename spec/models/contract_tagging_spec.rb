require 'rails_helper'

RSpec.describe ContractTagging, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:contract_tagging)
  #   expect(record).to be_persisted
  # end
end
