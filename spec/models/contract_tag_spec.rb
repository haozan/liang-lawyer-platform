require 'rails_helper'

RSpec.describe ContractTag, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:contract_tag)
  #   expect(record).to be_persisted
  # end
end
