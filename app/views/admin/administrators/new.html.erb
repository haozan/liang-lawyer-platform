<% content_for :title, "新建管理员" %>

<div class="container-md">
  <div class="flex items-center space-x-4">
    <%= link_to admin_administrators_path, class: "btn-ghost text-sm py-1 px-3" do %>
      <%= lucide_icon "arrow-left", class: "w-4 h-4 mr-2" %>
      返回管理员列表
    <% end %>
  </div>

  <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-50 mt-4 mb-8">创建新管理员</h1>

  <!-- Form -->
  <div class="card bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700">
    <div class="card-body">
      <%= form_with model: [:admin, @administrator], local: true, class: "space-y-6" do |form| %>
        <!-- Name -->
        <div class="form-group">
          <%= form.label :name %>
          <%= form.text_field :name, placeholder: "请输入管理员姓名..." %>
        </div>

        <!-- Phone -->
        <div class="form-group">
          <%= form.label :phone, "手机号" %>
          <%= form.telephone_field :phone, placeholder: "请输入11位手机号", required: true %>
        </div>

        <!-- Password -->
        <div class="form-group">
          <%= form.label :password %>
          <%= form.password_field :password, placeholder: "请输入密码..." %>
        </div>

        <!-- Password Confirmation -->
        <div class="form-group">
          <%= form.label :password_confirmation %>
          <%= form.password_field :password_confirmation, placeholder: "确认密码..." %>
        </div>

        <!-- Role (only for super admins) -->
        <% if current_admin.can_manage_administrators? %>
          <div class="form-group">
            <%= form.label :role %>
            <%= form.select :role, Administrator.role_options, { selected: @administrator.role || 'admin' } %>
          </div>
        <% end %>

        <!-- Form Actions -->
        <div class="flex justify-end space-x-3">
          <%= link_to admin_administrators_path, class: "btn-ghost" do %>
            取消
          <% end %>
          <%= form.submit "创建管理员", class: "btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
