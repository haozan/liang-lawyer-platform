<% content_for :title, "新建合同标签" %>

<div class="container-md">
  <div class="flex items-center space-x-4">
    <%= link_to admin_contract_tags_path, class: "btn-ghost text-sm py-1 px-3" do %>
      <%= lucide_icon "arrow-left", class: "w-4 h-4 mr-2" %>
      返回合同标签列表
    <% end %>
  </div>

  <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-50 mt-4 mb-8">新建合同标签</h1>

  <!-- Form -->
  <div class="card bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700">
    <div class="card-body">
      <%= form_with model: [:admin, @contract_tag], local: true, class: "space-y-6" do |form| %>
        <!-- Name -->
        <div class="form-group">
          <%= form.label :name %>
          <%= form.text_field :name, placeholder: "请输入标签名称..." %>
        </div>

        <!-- Color -->
        <div class="form-group">
          <%= form.label :color %>
          <%= form.text_field :color, placeholder: "请输入颜色代码..." %>
        </div>

        <!-- Company -->
        <div class="form-group">
          <%= form.label :company_id, "所属企业" %>
          <%= form.select :company_id,
              options_for_select([['请选择企业', '']] + Company.all.map { |item| [item.name, item.id] }, @contract_tag.company_id) %>
        </div>

        <!-- Form Actions -->
        <div class="flex justify-end space-x-3">
          <%= link_to admin_contract_tags_path, class: "btn-ghost" do %>
            取消
          <% end %>
          <%= form.submit "创建标签", class: "btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>